# build artifacts
*.o
*.so
*.dylib
ariannamethod/kairos_train
*.bin

# internal logs (KAIROSLOG.md is the tracked one)
LOCAL_STATE.md
PROJECT_LOG.md

# runtime / ecology state
.kairos/
*.ckpt
*.sqlite3
